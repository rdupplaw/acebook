<% @posts.each do |post| %>
  <p><%= post.message %></p>
  <p><%= post.comments.length %> comments</p>
  <%= link_to "View", post_path(post) %><br>
<% end %>

<%= link_to "New post", new_post_path %>
