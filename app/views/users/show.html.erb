<%= @user.firstname %> 
<%= @user.lastname %>, 
<%= @user.email %>, 
<%= image_tag profile_picture_for(@user), class: "profile-pic" %>

<div class="content">
  <% @user.posts.each do |post| %>
    <% if @user == post.user %>
      <%= render post %>
    <% end %>
  <% end %>
</div>