<div class="content">
  <h2>Friends List</h2>
  <% @invitations.each do |friend| %>
    <%= link_to "#{User.where(id: friend.friend_id).first.firstname} #{User.where(id: friend.friend_id).first.lastname}", user_path(friend.friend_id), method: :get %> <br>
  <% end %>
</div>