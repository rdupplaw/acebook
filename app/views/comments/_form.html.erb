<%= form_with model: [ @post, @post.comments.build ], local: true do |form| %>
    <p class="commentarea">
      Add your comment here: <br><br>
      <%= form.text_area :body %>
    </p>
    <p>
      <%= form.submit %>
    </p>
  <% end %>